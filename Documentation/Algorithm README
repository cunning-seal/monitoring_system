Клиентские метрики БД
1) При помощи SQLDriver определяется время ответа на запрос. Данные записываются в файл temp1.txt в формате, подобном json:
[
	{'request':value, 'time':value},
	...
]
2) Агент на каждой своей итерации считывает данные из temp1.txt. В этот момент файл пишет в temp2.txt (т.е. когда fopen вернёт ошибку). После считывания данных агент очищает файл и оставляет []. Аналогично, в следующий раз агент будет считывать из temp2.txt, а драйвер будет писать в temp1.txt
3) Данные отправляются на UDP-сервер как и остальные метрики 

